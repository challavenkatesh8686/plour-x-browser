# Same Supabase project as every other PlourX app — one PlourX account across apps.
# Copy this file to .env and fill in the real values (see plour-x-ai/client/.env).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
